%p{:id => "notice"}= notice

%h1 Listing Projects

%table
  %thead
    %tr
      %th Name
      %th Git repo
      %th Pull Requests
      %th Slack
      %th{:colspan => "3"}

  %tbody
    - @projects.each do |project|
      %tr
        %td= project.name
        %td= project.git_repo
        %td= graphic project.pull_request
        %td= graphic project.slack_metric
        %td= link_to 'Show', project
        %td= link_to 'Edit', edit_project_path(project)
        %td= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Project', new_project_path
