%p{:id => "notice"}= notice

%h2 Project Listing

%table
  %thead
    %tr
      %th Name
      %th Git repo
      %th Slack
      %th Pull Requests
      %th{:colspan => "3"}

  %tbody
    - @projects.each do |project|
      %tr
        %td= project.name
        %td= project.git_repo
        %td= raw(slack_graphic(project.slack_metric))
        %td= raw(pull_request_graphic(project.pull_request))
        %td= link_to 'Show', project
        %td= link_to 'Edit', edit_project_path(project)
        %td= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'Add a Project', new_project_path
